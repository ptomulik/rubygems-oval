source 'https://rubygems.org'

group :development, :test do
  gem 'rspec-core'
  gem 'rspec-expectations'
  gem 'mocha'
  if RUBY_VERSION >= "1.9"
    gem 'coveralls', :require => false
    gem 'yard'
    gem 'redcarpet'
    gem 'github-markup'
  end
end

gemspec

# vim: ft=ruby
